<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Forecast - <%= city %></title>
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-blue-100 min-h-screen p-6">
  <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-lg">
    <h1 class="text-2xl font-bold text-center mb-4">พยากรณ์อากาศ: <%= city %></h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% weatherData.list.slice(0, 7).forEach(forecast => { %>
        <div class="bg-blue-50 p-4 rounded-lg text-center shadow-md">
          <p class="font-semibold"><%= new Date(forecast.dt_txt).toLocaleString() %></p>
          <p>อุณหภูมิ: <%= forecast.main.temp %>°C</p>
          <p>สภาพอากาศ: <%= forecast.weather[0].description %></p>
          <img
            src="http://openweathermap.org/img/wn/<%= forecast.weather[0].icon %>@2x.png"
            alt="weather icon"
            class="mx-auto"
          />
        </div>
      <% }); %>
    </div>
    <div class="mt-6 text-center">
      <a href="/" class="text-blue-500 underline">กลับไปหน้าหลัก</a>
    </div>
  </div>
</body>
</html>
